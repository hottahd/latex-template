%% ===========================================================================
%%  「修士論文を書くにあたって」だけを 1 冊の PDF にするためのファイル。
%%
%%      latexmk guide-only.tex
%%
%%  中身は main.tex をそのまま読んでいます。設定も引用文献も本編と同じものを
%%  使うので、guide.tex を直せばこちらにも自動で反映されます。
%%  GitHub Actions がこれを使って guide-only.pdf を作っています。
%% ===========================================================================
\def\GuideOnly{}
\input{main}
